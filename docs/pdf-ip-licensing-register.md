# PDF IP, Licensing, And Standards Register

**Register version:** 1.6  
**Updated:** 2026-09-01 (IP-021 and SRC-001 closed on inspection)  
**Owner:** Broiler.Documents maintainers  
**Approval authority:** Qualified legal reviewer designated by the project

This register is an engineering control, not legal advice. `Pending` is a
blocking state for implementation or public claims involving that row. A public
patent declaration or patent license is recorded as evidence, not interpreted as
worldwide freedom to operate.

## Relationship to the implemented code

`Broiler.Documents.Pdf` implements the base slice described in
[roadmap §2.5](pdf-support-roadmap.md#25-current-implementation-state). Three
things follow for this register. Exactly one of them is a clearance, and it is
narrower than the technology it names.

First, the scope was chosen to keep the number of live rows small: the base build
implements only syntax, structure, the Flate/ASCIIHex/ASCII85/RunLength filters,
and the XMP read subset cleared under IP-004. It carries no third-party runtime
dependency and bundles no font, glyph list, metric file, ICC profile, or codec
asset. **Every filter and codec row in this register has now been decided.**
Nothing in the image and compression family is blocked or pending any longer.

What is left is not clearance but engineering, and the two rows that show it
most plainly are IP-007 and IP-008. Both are approved and neither decodes the
thing a real document usually contains: JPEG 2000 has no decoder at all, and
JBIG2 has one only for generic regions coded with MMR, while almost every JBIG2
image in a scanned PDF is a symbol dictionary and text regions under the
arithmetic coder. Both report what they found instead, precisely enough that the
remaining work can be sized from a corpus rather than guessed at.

IP-007 is the row that separates approval from capability most sharply. JPEG 2000
Part 1 is cleared, and the codec still does not decode it: what is written is a
codestream *reader* that reports exactly which image a `JPXDecode` stream holds,
and what is not written is the arithmetic coder, EBCOT, and the wavelet
transforms. That gap is engineering rather than clearance, and the diagnostic says
so in those words, because "we are not allowed to" and "nobody has written it yet"
are fixed by entirely different work and a host should never have to guess which
one it has hit.

IP-009 is the first row where clearing the patent question was not the whole
question. Fax coding is patent-retired, and the decoder is this repository's own,
but its run-length codes could not be *authored* the way every other table here
was: a Huffman assignment has no underlying fact to derive it from, so the
constants are transcribed from ITU-T T.4. That is a copyright question about the
specification rather than a patent question about the technology, the evidence
record does not reach it, and it is carried as an open source item (SRC-017)
instead of being treated as answered.

IP-010 is the row that shows what clearing one is actually for. LZW went into the
**base build**, next to Flate and RunLength, rather than behind the composition
boundary the other cleared rows sit behind. The boundary exists for three reasons
(extension points §1) and LZW, once cleared, meets none of them: there is no
outside component to review, because the decoder is a page of this repository's
own code with no table or asset; and the security case for keeping a codec out of
the default build is about image decoders and font parsers, not about a bounded
byte-stream decompressor of exactly the shape the base build already carries four
of. A cleared row does not automatically mean "compose it" — it means the reason
for the boundary has to be re-asked.

IP-005 and IP-006 are the exception, and only just: baseline JPEG and its colour
declaration are approved and implemented, but not *here*. The decoder is
`Broiler.Media`'s, the adapter that gives it a PDF budget, a tuple check, and a
colour resolution is `Broiler.Documents.Pdf.Images`, and neither is composed
unless a host asks. A default build still links no image decoder, which is the
security position [extension points §1](pdf-extension-points.md) sets out and
which an approved patent row does not change.

The two JPEG rows are a worked example of why they are two rows. IP-005 says the
coding process may be decoded; IP-006 says the colour declaration may be read.
Approving one never implied the other, and holding them apart is what let the
codec ship baseline decoding for a fortnight's worth of files while still
refusing every image that declared its colour space.

IP-012 is approved for **inspection** and carries the same shape of limit, but
from a different direction. Nothing blocks reading an embedded program any more;
what bounds it is that the parser this repository already owns is a *renderer*.
It answers "which glyph draws this character" and exposes no glyph names, no
`post` table, and no `OS/2`, so the composed reader recovers text for sfnt
programs and declines Type 1 and bare CFF. Widening that is engineering in
`Broiler.Graphics`, not a register decision — and it would re-open that
component's own human review, which is pinned to a reviewed revision.

Second, IP-004 is the first row in this register to be **approved**, on
2026-09-01, and only for the read subset its row names. The base build now parses
an XMP packet into the normalized metadata allowlist. It still preserves no raw
packet and writes no XMP, and that has never been a clearance question: the
roadmap excludes raw-packet carriage from V1 by design (§6.2).

Third, IP-001 — the row under every construct the codec implements — is now
**approved**, which removes the single largest obstacle to a support claim and
does not by itself create one. What still stands between this implementation and
a `Supported` entry is set out in §"What still blocks a support claim" below. The
package remains unpacked and reaches an application only as the read-preview
candidate. Implementation ahead of clearance was permitted here only because
nothing is published; publication is gated by the roadmap's Phase 5, 7, and 8 exit
criteria, which are engineering gates and unaffected by any of this.

Adding an implementation for any pending row follows the step order in
[PDF extension points §5](pdf-extension-points.md#5-adding-a-technology-step-by-step):
the row clears first, the capability moves last.

## Decision fields

Each row must eventually identify the exact feature/subset, specification
edition, source and acquisition right, implementation jurisdictions, patent
evidence, copyright/license conditions, reviewer, decision date, expiry/review
date, and obligations. Changes to scope reopen the row.

| ID | Technology / exact scope | Primary evidence | Current assessment | Status / required action |
|---|---|---|---|---|
| IP-001 | ISO 32000-1:2008 / PDF 1.7, the subsets enumerated in the feature matrix and inventoried in the construct inventory | Adobe Systems Incorporated [Public Patent License for ISO 32000-1:2008 / PDF 1.7](https://www.adobe.com/pdf/pdfs/ISO32000-1PublicPatentLicense.pdf); the standard, lawfully obtained | The licence grants a worldwide, royalty-free right under all Adobe-owned Essential Claims to make, have made, use, sell, import, and distribute **Compliant Implementations**, and reading, writing, modifying, and processing PDF files are each within that. No royalty identified for Adobe Essential Claims. Adobe's known historical PDF patents are additionally expected to have expired by 2019, so the position rests on a licence and on age independently. Risk assessed **very low**. The licence covers Adobe-owned claims only and does not establish the absence of third-party claims; the third-party technologies PDF references are reviewed on their own rows. | **Approved 2026-09-01.** Not retired: the grant is conditioned on being a Compliant Implementation, which is a live obligation rather than a one-time finding. **The obligation it creates:** the claim to be one rests on the feature matrix and the construct inventory describing what actually ships, so those documents are part of the clearance and not commentary on it. `PdfClaimGuardTests` binds them to the code mechanically, and it found fourteen undeclared diagnostics on the day this row cleared. Still unrecorded: implementation jurisdictions and the expiry/review date. |
| IP-002 | ISO 32000-2 tolerance or features, including amendments | Lawfully obtained applicable editions; [ISO standards and patents policy](https://www.iso.org/iso-standards-and-patents.html) | IP-001 cannot be extended by inference. ISO states declaration data are informational and not verified for accuracy or relevance. | **Pending qualified review.** Do not claim PDF 2.0 conformance. Enumerate tolerated syntax separately from implemented features. |
| IP-003 | Adobe PDF extensions or implementation notes | Exact extension document and its terms | Public availability is not implementation permission and an extension may sit outside IP-001. | **Pending.** No extension is in scope until separately registered and approved. |
| IP-004 | XMP (Extensible Metadata Platform) **read** path only: ISO 16684-1:2019, restricted to the RDF/XML serialization subset and the nine `dc`/`xmp`/`pdf` properties implemented in `Broiler.Documents/XmpMetadata.cs`. Writing XMP and preserving raw packets are outside the row. | ISO 16684-1:2019 (originator Adobe); Adobe's public royalty-free patent licence for XMP; [Adobe XMP Toolkit SDK](https://github.com/adobe/XMP-Toolkit-SDK) as SRC-005, issue spotting only | A patent notice is present and the holder is Adobe, licensed royalty-free for implementations; no implementation royalty identified. Specification text is copyrighted as an ISO publication, so it is cited by clause and never reproduced. The reader is authored in this repository from the structure of the standard; no toolkit code, table, fixture, or test vector was copied, so the BSD code licence of the SDK is not relied on and its distinctness from the specification/patent scope does not arise. Risk assessed **low**. | **Approved 2026-09-01** for the read subset above, per the evidence record supplied by the project maintainer (legal review: OK). Changing the subset — writing XMP, preserving raw packets, adding schemas or namespaces — reopens the row. Still unrecorded on this row, and required before it can support a public claim: implementation jurisdictions, the expiry/review date, and any attribution obligation attaching to the ISO publication licence. |
| IP-005 | JPEG-1 **baseline sequential DCT** as used by `DCTDecode`: ISO/IEC 10918-1:1994 / ITU-T T.81, Huffman entropy coding, 8-bit sample precision, 1 or 3 components. Colour interpretation is IP-006's, not this row's. Decoder source: the project-owned managed decoder in `Broiler.Media.Image.Managed` (SRC-007). | ISO/IEC 10918-1:1994 / ITU-T T.81 and its Annex L patent notice; [ITU-T T.81 declarations](https://www.itu.int/ITU-T/recommendations/related_ps.aspx?id_prod=2633) | A patent notice exists (historical, Annex L) and certain coding processes carried RAND terms; baseline is royalty-free and the JPEG-1 patents are recorded as expired, with no implementation royalty identified and open-source implementation permitted. Risk assessed **very low**. The row is written as tuples rather than as "JPEG" because the technology is not one indivisible clearance: the arithmetic-coded processes are the part the historical RAND terms attached to, and they stay out. Specification text is a copyrighted ISO/ITU publication, cited by clause and never reproduced. | **Approved 2026-09-01** for the baseline tuple above. Progressive DCT (SOF2) is *not* covered and reports `pdf.image.dct.progressive-unsupported`, even though the composed decoder implements it — widening the row is a decision, not a code change. Arithmetic, lossless, hierarchical, differential, 12-bit, and 4-component frames stay refused. Still unrecorded: implementation jurisdictions and the expiry/review date. |
| IP-006 | JPEG Adobe `APP14` marker (`FF EE`, identifier `Adobe`) and the `ColorTransform` value it carries, plus the PDF `/ColorTransform` entry that states the same thing: reading the declaration and choosing the colour rule from it. Values 0 (no transform), 1 (YCbCr), 2 (YCCK). | Adobe Technical Note #5116, *Supporting the DCT Filters in PostScript Level 2*; ISO 32000-1 clause 7.4.8 for the `/ColorTransform` entry | Adobe-originated marker convention rather than a standardized coding technology. No implementation royalty identified. Widely implemented in the open — libjpeg / libjpeg-turbo, Apache Commons Imaging, Java ImageIO — which is recorded as evidence that implementing it is ordinary practice and **not** as permission to copy any of them (SRC-015). Patent/licensing risk assessed **very low**. The marker's semantics are a fact about a format; the code expressing them is this project's. | **Approved 2026-09-01.** The composed filter reads both declarations, decodes transform 1 and the 3-component default, and refuses the rest by name. Two refusals survive this approval and are *not* clearance questions: transform 0 on three components is refused because the composed decoder applies the YCbCr conversion unconditionally, and YCCK is refused because four-component conversion is outside V1 scope (roadmap §1.1). Contradictory declarations report `pdf.image.dct.color-transform-uncertain` rather than being resolved. Still unrecorded: implementation jurisdictions and the expiry/review date. |
| IP-007 | JPEG 2000 **Part 1 core coding system** as used by `JPXDecode`: ISO/IEC 15444-1 (original edition 2000; current 5th edition 2024) / ITU-T T.800. Part 2 (JPX) extensions are **not** in this row. | ISO/IEC 15444-1 / ITU-T T.800; the JPEG committee's published statements on royalty-free licensing commitments by Part 1 contributors | Historical patent declarations exist, and **patent-freedom is not claimed**. The clearance rests on two independent bases instead: the royalty-free patent-licensing commitments the JPEG committee records for Part 1 contributors, and the age of the original core patents, which are additionally expected to have expired given a 2000 publication. No implementation royalty identified for a Part 1 core implementation. Risk assessed **low**. | **Approved 2026-09-01 for the Part 1 core coding system.** Deliberately *not* retired: unlike IP-009 and IP-010, this row does not rest on expiry alone, so it stays live and subject to re-review rather than closing. Implemented so far: a composed codestream reader that reports the image tuple and refuses Part 2 by `Rsiz`. Not implemented: the entropy decoder. Still unrecorded: implementation jurisdictions, the expiry/review date, and — should a decoder be written — whether reproducing the MQ-coder state and context tables falls under SRC-017's open question. |
| IP-008 | JBIG2 as used by `JBIG2Decode`: ITU-T T.88 / ISO/IEC 14492 (original T.88:2000 and 14492:2001; current T.88:2018 and 14492:2019 edition 2). | ITU-T T.88 / ISO/IEC 14492; the ITU/ISO patent declarations, whose declaring parties include Xerox, KDDI, Mitsubishi Electric, IBM, Kwangwoon University, and AIST | Historical patent declarations exist and are numerous, so **patent-freedom is not claimed**. The clearance rests on the age of the declarations — filed 1999 to 2003 and expected expired — together with the Encoder Amendment's deliberate selection of example methods drawn from expired or expiring patents, openly published methods, or royalty-free ones. No royalty identified for a conventional implementation. Risk assessed **low**. | **Approved 2026-09-01.** Not retired: the declaration set is broad and the row rests on expected expiry rather than a licence, so it stays live and subject to re-review. Implemented so far: the segment structure, and generic regions coded with MMR, which decode through the T.6 decoder cleared under IP-009. Not implemented: the MQ arithmetic decoder and everything that needs it — symbol dictionaries, text regions, halftone and refinement regions — which is most of what real documents contain. Still unrecorded: implementation jurisdictions and the expiry/review date. |
| IP-009 | PDF `CCITTFaxDecode`: ITU-T T.4 and T.6 fax coding — Modified Huffman (MH), Modified READ (MR), and Modified Modified READ (MMR) — as ISO 32000-1 clause 7.4.6 parameterizes them. Decoder written in this repository. | ITU-T T.4 (1980, amended 1984/1988) and T.6 (1984/1988); the ITU patent declaration database | Patent declarations exist or existed, and ITU states it does not verify their essentiality, completeness, or validity. The original rights relevant to these algorithms are expired by age given the standards' dates. No current implementation royalty identified. Patent risk assessed **very low**; the row's status is recorded as retired. | **Approved 2026-09-01 for the patent position, and that part of the row is retired.** All three algorithms implemented, composed rather than built in: a bit-level entropy decoder producing a pixel buffer sized from the dictionary is the attack surface the composition boundary exists for, unlike LZW. **Not answered by this row:** whether reproducing T.4's run-length code tables is within the specification's terms. They are the only transcribed normative constants in this repository, unavoidably so, and SRC-017 carries that question. Still unrecorded: implementation jurisdictions. |
| IP-010 | PDF `LZWDecode`: Lempel-Ziv-Welch as ISO 32000-1 clause 7.4.4 defines it, including the `EarlyChange` parameter. Decoder written in this repository. | US 4,558,302 (Welch / Sperry / Unisys) and the other historical LZW patents; the published expiry record for each | The patents this technology was ever encumbered by are recorded as expired: Unisys in the United States in 2003, Europe, Japan and Canada in 2004, and IBM's in the United States in 2006, with the later implementation patents identified also expired. No current implementation royalty identified. Patent risk assessed **very low**; the row's own status is recorded as retired rather than merely cleared. | **Approved 2026-09-01, and the row is retired.** Implemented as a built-in filter rather than a composed one: with no outside component to review and no image-codec-scale attack surface, the composition boundary had nothing left to hold. Retained: `pdf.filter.lzw.unsupported` stays in the code table as API for a caller who replaces the built-in with a filter that declines. Still unrecorded: implementation jurisdictions, though a retired row is the one case where that matters least. |
| IP-011 | `FlateDecode` and the predictor algorithms: the zlib format (RFC 1950), DEFLATE (RFC 1951) and its LZ77 and Huffman components, TIFF predictor 2, and the PNG predictors — None, Sub, Up, Average, and Paeth | RFC 1950 and RFC 1951; the PNG predictor definitions; ISO 32000-1 clause 7.4.4.4 | RFC 1950 and RFC 1951 each state that the format can readily be implemented in a manner not covered by patents, and PNG was designed as patent-free technology. No third-party decoder licence is required and no royalty is identified. Risk assessed **very low**. Provenance: the codec adds no compression implementation of its own — DEFLATE is the platform's, under IP-023 — and the predictors are implemented in this repository from the specifications, carrying no table, constant, or test vector from any other implementation. | **Approved 2026-09-01.** The row asked for two things and both are answered: the algorithm position by the RFCs' own statements, and the implementation provenance by there being no third-party implementation in the codec at all. Completed on the same day: TIFF predictor 2 now covers 1-, 2-, and 4-bit components, which had been refused for want of a fixture rather than for want of clearance. Still unrecorded: implementation jurisdictions. |
| IP-012 | Reading embedded font programs to recover what their glyphs mean: Type 1, TrueType, OpenType, CFF, CFF2. **Inspection only.** Embedding a font into output, subsetting one, and redistributing one are expressly outside this row. | [Microsoft OpenType specification](https://learn.microsoft.com/en-us/typography/opentype/spec/) (SRC-006); Adobe CFF / Type 2 specifications; ISO/IEC 14496-22 Open Font Format (SRC-016) | Format specifications with no implementation royalty identified; patent/licensing risk assessed **low**. Font-format implementation and font-content rights stay separate concerns, which is why this row is written as inspection: reading a program a document already embedded, in order to say what its glyphs spell, neither copies the font nor redistributes it. | **Approved 2026-09-01 for inspection.** Implemented for sfnt programs (`FontFile2`, and `FontFile3` `/OpenType`) through a composed reader; Type 1 and bare CFF are declined because the composed parser exposes no glyph names, which is a capability limit rather than a clearance one. **Obligations carried forward, not discharged:** an individual font's embedding permissions, including the OpenType `OS/2` `fsType` flags, must be respected by anything that embeds or subsets. Nothing does today — this release embeds no font and bundles none — so the obligation attaches to the writer work that would first need it, and this row must be re-opened before that work starts. Still unrecorded: implementation jurisdictions and the expiry/review date. |
| IP-013 | Unicode mapping, normalization, bidirectional processing, and character-property data: The Unicode Standard 17.0, UAX #9, UAX #15, and the Unicode Character Database | The Unicode Standard 17.0 and the UCD, under the Unicode License v3; the Unicode Consortium's royalty-free patent policy | The licence permits use, modification, and distribution **subject to its notice requirements**, and the Consortium operates a royalty-free patent policy; no royalty is identified and no third-party runtime library is required. Risk assessed **very low**. What the codec actually consumes today is narrower than the row: character identities, and the .NET runtime's own Unicode APIs. It performs no normalization, no bidirectional processing, and carries **no table derived from a UCD file**. | **Approved 2026-09-01.** **The obligation this row creates:** the licence's notice requirement attaches to any distribution of UCD-derived data. None ships today — the encoding tables are authored under IP-021 from character identity rather than generated from UCD files — so nothing is due yet, and the notice becomes required the moment a generated table lands. That is a release-artifact obligation and belongs with the component's third-party notices, not with this codec's source. Unblocked on the day it cleared: the Symbol font's built-in encoding, which had been reported as unmappable. Still unrecorded: implementation jurisdictions and the expiry/review date. |
| IP-014 | URI syntax and scheme handling | The .NET runtime's `System.Uri` (SRC-011); this project's own scheme policy | **This row never asserted a patent question and does not decide one.** It asked a provenance question — which sources informed the URI handling, and whether any standards text or test vector was copied — and the answer is that there is no URI implementation here to trace. The codec parses no URIs: `PdfUriPolicy` checks length and control characters, hands the value to `System.Uri.TryCreate`, and compares the resulting scheme as an ordinal string. No ABNF, no percent-decoding table, no grammar, and no test vector from RFC 3986 or anywhere else appears in this repository. What remains of the row is the scheme allow-list, which is this project's own decision and is documented and tested as such. | **Approved 2026-09-01 on engineering source review**, on the same footing as IP-011's provenance half: consuming a platform API satisfies an implementation-provenance requirement precisely because there is nothing imported to account for. No legal question was open on this row and none is decided by closing it. |
| IP-015 | Standard security handler / encryption | Exact ISO clauses and cryptographic specifications | Patent, export-control, security, and interoperability review not completed. | **Blocked for V1.** Encrypted inputs are rejected. |
| IP-016 | Digital signatures and certificate validation | Exact ISO clauses, cryptographic standards, trust-store behavior | Signature preservation and validation claims create substantial security and legal scope. | **Blocked for V1 / Post-V1.** Separate architecture and qualified review required. |
| IP-017 | PDF/A, PDF/UA, PDF/X or other profiles | Each exact profile edition, normative dependencies, validation and mark rules | Base-PDF work does not grant profile conformance. | **Blocked for V1 / Post-V1.** Separate standards acquisition, legal review, and conformance plan required. |
| IP-018 | “PDF” naming, Adobe references, certification or compatibility claims | The approved label set in [Approved labels](#approved-labels); the project's standing claims rule (roadmap §5.1) | Descriptive use of the format's name is what the labels are for, and none of them names a vendor, asserts a certification, or implies sponsorship. The rule they enforce is the negative one: nothing describes this implementation as patent-free, unconditionally royalty-free, certified, endorsed, or fully conforming. | **Approved 2026-09-01** for those labels and no other wording. Applied on the day it cleared: the package description had shipped the phrase “patent-free filter set”, which the roadmap's claims rule forbids and which this register contradicts on every codec row. It is corrected, and `PdfClaimGuardTests` now fails the build on it rather than trusting anyone to notice. |
| IP-019 | Old Broiler PDF source, binaries, fixtures, generated data, or output goldens | Per-artifact origin, author, license, hashes, and approval | None are approved sources merely because they are in project history or a local workspace. | **Rejected by default.** Delete/ignore; import only after independent approval and provenance record. |
| IP-020 | User/third-party PDFs, images, and fonts used as fixtures | Written grant/license, redistribution scope, privacy review | Possession or public download is not permission to commit or redistribute. | **Pending per artifact.** Prefer purpose-built generated fixtures once the generator sources are approved. |
| IP-021 | Broiler-authored PDF character data: the `StandardEncoding`/`WinAnsiEncoding`/`MacRomanEncoding` code-point tables, the Symbol font's built-in encoding, the Latin glyph-name repertoire, and PDFDocEncoding's exceptional ranges | `Broiler.Documents.Pdf/Text/PdfEncodings.cs`, `Structure/PdfMetadataReader.cs`; source record SRC-010 | Authored from the character identity each encoding slot denotes rather than transcribed from a third-party glyph-list file or standard table, which inspection confirms: no data file exists in the source tree, the tables are character literals in code, and the glyph names are derived where they can be derived. One fact distinguishes this data from the transcription SRC-017 carries as an open question — these encodings are independently and publicly documented outside ISO 32000-1, WinAnsi as Windows-1252 and MacRoman as Mac OS Roman, whereas T.4's Huffman codes exist essentially only in T.4. | **Approved 2026-09-01 on engineering source review**, closing the last of the three rows that covered this data: SRC-010 approved it as a source and IP-022 approved its metric twin on the same authoring position, leaving this one the odd row out. The wording restriction IP-022 carried is now enforced mechanically under IP-018. `PdfClaimGuardTests` fails the build if a data file appears beside the codec, so the authored-not-copied position is now a property of the tree rather than a claim about it. |
| IP-022 | Broiler-authored approximate font metrics used for writer line breaking | `Broiler.Documents.Pdf/Text/PdfFontMetrics.cs`; source record SRC-010 | A proportion-class model authored from Latin letterform proportions. It is deliberately **not** Adobe's Standard 14 AFM metrics, and output must never be described as metrically exact or as using any vendor's metrics. Real metrics arrive through `IPdfFontMetricsProvider` under IP-012. | **Approved for use as authored data**; the accompanying wording restriction is a claims-review item. |
| IP-023 | The .NET runtime's DEFLATE/zlib implementation used for `FlateDecode` | `System.IO.Compression.ZLibStream`; the platform's own licence and notices | The codec adds no compression implementation, table, or test vector of its own; it calls the runtime. This is a platform dependency, not a bundled third-party component, and it carries no PDF-specific obligation. | **Confirmed 2026-09-01 under IP-011.** Consuming a platform API satisfies that row's implementation-provenance requirement precisely because there is no imported implementation to trace: the obligation travels with the runtime's own notices, as it does for every other framework API this codec calls. |

## Approved labels

Approved 2026-09-01 under IP-018. These are the wordings this component and any
application built on it may use for the format. Anything else is a new decision.

| Context | Approved label |
|---|---|
| Format list | **PDF** |
| Save As | **PDF Document (*.pdf)** |
| Import | **PDF** |
| Export | **PDF** |
| Tooltip / Help | **Portable Document Format (PDF)** |
| Technical documentation | **PDF (ISO 32000)** |
| Specific PDF 1.7 reference | **PDF 1.7 (ISO 32000-1)** |
| Specific PDF 2.0 reference | **PDF 2.0 (ISO 32000-2)** |

Two things follow that the table does not say outright. The labels are
*descriptive*: each names the format, none names a vendor, and none asserts a
certification or a compatibility relationship with any product. And the PDF 2.0
label is a reference form only — this codec makes no ISO 32000-2 conformance
claim, and IP-002 remains pending, so the label describes a version a document
declares rather than a capability this build has.

## What still blocks a support claim

IP-001 clearing is the largest single step and not the last one. As of
2026-09-01 a `Supported` entry still waits on:

| Blocker | Kind | State |
|---|---|---|
| SRC-017 | Reproducing ITU-T T.4's code tables | Pending source/copyright review |
| Roadmap Phase 5, 7, 8 | Read-preview, writer, and hardening exit criteria | Engineering gates, unaffected by any register row |

None of these is a codec question. Every filter and codec row is decided; what
remains is provenance, wording, and delivery.

## Review record

| Review | Reviewer | Date | Scope | Result |
|---|---|---|---|---|
| Engineering issue spotting | Codex, not legal counsel | 2026-08-22 | Phase 0 architecture and public primary-source pointers | Register created; no row legally cleared |
| Base-implementation scope review | Engineering review, not legal counsel | 2026-08-25 | Which rows the implemented base slice exercises; IP-021 to IP-023 added | Live rows narrowed to IP-001, IP-011, IP-013, IP-014; no row legally cleared, and nothing published |
| XMP standards, patent, and provenance review (IP-004) | Evidence record supplied by the project maintainer | 2026-09-01 | ISO 16684-1:2019; the RDF/XML read subset and the nine normalized properties | **Approved.** First row in this register to clear. The read subset was implemented afterwards, in that order. Jurisdictions, expiry/review date, and ISO attribution obligations were not part of the record and remain open on the row. |
| JPEG-1 standards and patent review (IP-005) | Evidence record supplied by the project maintainer | 2026-09-01 | ISO/IEC 10918-1:1994 / ITU-T T.81; baseline sequential DCT | **Approved for baseline.** The record names baseline as royalty-free and JPEG-1 patents as expired; the row was written to the tuples that statement supports and no further. Progressive DCT and the IP-006 colour rules were not covered and remain refused in code. Jurisdictions and the expiry/review date were not part of the record. |
| ISO 32000-1 / PDF 1.7 review (IP-001) | Evidence record supplied by the project maintainer | 2026-09-01 | ISO 32000-1:2008 and Adobe's public patent licence for it | **Approved.** The row every implemented construct sits under. Recorded as conditioned rather than closed: the grant reaches Compliant Implementations, so the matrix and inventory that define which implementation this is became part of the clearance, and a guard test now binds them to the code. Fourteen diagnostics turned out to be undocumented and were inventoried the same day. |
| Character-data source review (IP-021) | Engineering review completed | 2026-09-01 | `PdfEncodings.cs` and the data it builds | **Approved.** Inspection settles the authored-not-copied position on facts: the source tree contains no data file of any kind, the encoding tables are built from character literals grouped in runs, the glyph-name repertoire is derived in loops for the alphabets and named individually only for ASCII punctuation, and the `uniXXXX` forms are computed rather than tabulated. A transcribed glyph list would be a flat several-thousand-entry mapping; this is 466 lines carrying three encodings, the Symbol table, and the repertoire together. Names that denote a glyph index rather than a character are refused outright, which is the character-identity discipline visible in the code. |
| ISO 32000 source-use review (SRC-001) | Engineering review completed | 2026-09-01 | Everything in this component that was written from the standard | **Approved for ISO 32000-1:2008.** No standard text, table, figure, or excerpt is committed anywhere in the repository, and no PDF 2.0-only construct is implemented — the single `IsPdf2OrLater` branch records a declaration and reads the file as ISO 32000-1 constructs while disclaiming ISO 32000-2 conformance. The priced edition was therefore not needed for any implemented behavior, and the row is closed for the freely published edition rather than for "ISO 32000 editions" at large; consulting ISO 32000-2 would reopen it. |
| Naming and claims review (IP-018) | Label set supplied by the project maintainer | 2026-09-01 | Format naming across format lists, dialogs, help text, and documentation | **Approved for the recorded label set.** Applying it found the one place a prohibited claim would actually have reached a user: the package description said "patent-free filter set", contradicting every codec row in this register and the roadmap's own claims rule. Corrected, and now guarded mechanically — a claims rule that lives only in prose is one waiting to be violated by whoever writes the next description field. |
| URI handling source review (IP-014) | Engineering review completed | 2026-09-01 | Every URI path in `Broiler.Documents.Pdf` | **Approved.** Inspection found a single `System.Uri.TryCreate` call and no other URI handling anywhere in the codec: no grammar, no decoding table, no copied test vector. The row's remaining substance — the scheme allow-list — is a project decision already documented and covered by tests. Recorded as an engineering finding about this repository's own source, which is what the row asked for; it is not a legal determination and none was needed. |
| Unicode review (IP-013) | Evidence record supplied by the project maintainer | 2026-09-01 | The Unicode Standard 17.0, UAX #9, UAX #15, the UCD, and the Unicode License v3 | **Approved, with a notice obligation carried forward rather than discharged.** The row is broader than what the codec consumes: no normalization, no bidirectional processing, and no UCD-derived table ships, so the licence's notice requirement is recorded against future generated data rather than against anything present. Reviewing it unblocked the Symbol font's built-in encoding and surfaced a defect next to it — ZapfDingbats had been falling through to the Latin fallback and extracting confident nonsense — which was fixed the same day. |
| Flate and predictor review (IP-011, IP-023) | Evidence record supplied by the project maintainer | 2026-09-01 | RFC 1950/1951, the PNG predictors, and TIFF predictor 2 | **Approved, and IP-023 confirmed with it.** The row's two halves closed for different reasons: the algorithm position on the RFCs' own patent statements, and the provenance half on the codec importing no compression implementation at all. Reviewing the row surfaced an incomplete capability rather than a legal one — TIFF predictor 2 refused sub-byte components — which was implemented the same day. |
| JBIG2 review (IP-008) | Evidence record supplied by the project maintainer | 2026-09-01 | ITU-T T.88 / ISO/IEC 14492 | **Approved, not retired**, on the same reasoning as IP-007: a broad declaration set with no patent-freedom claim, cleared on expected expiry plus the Encoder Amendment's royalty-free method selection. The last row in the filter and codec family to be decided. Implementation covers the segment structure and MMR generic regions only; the arithmetic decoder is recorded as scoped work. |
| JPEG 2000 Part 1 review (IP-007) | Evidence record supplied by the project maintainer | 2026-09-01 | ISO/IEC 15444-1 / ITU-T T.800, core coding system | **Approved, not retired.** The record is explicit that patent-freedom is not claimed, so the row clears on royalty-free commitments plus expected expiry and stays live. Part 2 was left outside the row and is refused in code by `Rsiz`. The decoder was not written: it is the largest codec in the filter set, has no baseline subset, and its failure mode is a plausible wrong image rather than an error — so it is recorded as scoped work rather than attempted alongside the clearance. |
| CCITT fax patent-history review (IP-009) | Evidence record supplied by the project maintainer | 2026-09-01 | ITU-T T.4 and T.6; MH, MR, and MMR | **Approved and retired on patents.** The evidence is an expiry-by-age record, so nothing carries forward on that axis. It does not address reproducing the standard's code tables, which the implementation could not avoid; that was registered as SRC-017 pending source/copyright review rather than assumed to follow from the patent clearance. First row in this register where the two questions came apart. |
| LZW patent-history review (IP-010) | Evidence record supplied by the project maintainer | 2026-09-01 | US 4,558,302 and the other historical LZW patents; PDF `LZWDecode` | **Approved and retired.** The evidence is an expiry record rather than a licence, which is why the row retires instead of carrying obligations forward. First cleared row to land in the base build rather than behind the composition boundary, on the reasoning recorded above. |
| Font-format review (IP-012) | Evidence record supplied by the project maintainer | 2026-09-01 | OpenType, CFF/Type 2, ISO/IEC 14496-22; embedded-program inspection | **Approved for inspection.** The record separates format implementation from font-content permissions, and the row was written to that split: inspection is approved, embedding and subsetting are not in scope and carry the `fsType` obligation to whatever work first needs them. Type 1 and bare CFF remain unread for want of parser surface, not for want of approval. Jurisdictions and the expiry/review date were not part of the record. |
| JPEG colour-transform review (IP-006) | Evidence record supplied by the project maintainer | 2026-09-01 | Adobe Technical Note #5116; the `APP14` marker and `ColorTransform` values 0, 1, 2 | **Approved.** Reading the declaration is now implemented. The record cited third-party implementations as evidence of practice; they were registered as issue-spotting sources only (SRC-015) and none was consulted for content. What the approval did not change: transform 0 on three components stays refused as a decoder-capability limit, and YCCK stays out of V1 scope. Jurisdictions and the expiry/review date were not part of the record. |
| Qualified legal review | _Unassigned_ | _Pending_ | Target jurisdictions and rows required by the first implementation slice | **Required for Phase 0 exit** |

