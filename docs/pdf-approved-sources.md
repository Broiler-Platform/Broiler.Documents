# PDF Approved Sources And Similarity Record

**Version:** 1.9  
**Updated:** 2026-09-02 (SRC-017's applicable terms gathered; row still pending)

Only sources with an `Approved` decision may inform implementation. A source may
be approved for background but not for copying code, prose, tables, fixtures, or
generated data. Contributors add a row before using a new implementation source.

## Source register

| ID | Source | Permitted use | Decision | Reviewer / evidence |
|---|---|---|---|---|
| SRC-001 | Lawfully obtained ISO 32000-1:2008 | Requirements research and clause citation; no reproduction of text, tables, or figures | Approved for ISO 32000-1:2008 | Closed 2026-09-01 on engineering inspection: nothing from the standard is committed, and no PDF 2.0-only construct is implemented, so the priced ISO 32000-2 edition was not required. Consulting a further edition reopens this row |
| SRC-002 | Adobe ISO 32000-1 Public Patent License | Identify license text and conditions; no implementation content | Approved for issue spotting only | Public primary document; legal interpretation pending in IP-001 |
| SRC-003 | ISO patent-policy page and declaration database | Locate declarations; never infer clearance | Approved for issue spotting only | Public primary source; IP-002 |
| SRC-004 | ITU-T T.81 recommendation/declaration pages | Locate exact JPEG recommendation and declarations | Approved for issue spotting only | Public primary source; IP-005 |
| SRC-005 | Adobe XMP Toolkit SDK repository | Identify published SDK license and separate spec-license link | Approved for issue spotting only | Public primary repository; no code may be copied without a new decision |
| SRC-006 | Microsoft OpenType specification page | Confirm the sfnt container, table directory, and `cmap` subtable formats; cite the specification. Do not reproduce its prose or tables | Approved for inspection use | IP-012 approved 2026-09-01. Approval covers reading a program, not embedding or subsetting one |
| SRC-007 | Existing Broiler shared-component source | Reuse through normal repository contribution history | Approved | Project-owned source; PDF-specific historical artifacts remain excluded |
| SRC-008 | Old Broiler PDF source, decompiled binaries, tests, fixtures, outputs | None | Rejected | ADR 0011 and IP-019 |
| SRC-009 | Third-party PDF libraries and their tests | None unless individually registered | Rejected by default | Prevent accidental code/test-vector similarity |
| SRC-010 | Broiler-authored PDF character and metric data (IP-021, IP-022) | Implementation use | Approved | Authored in this repository from character identities and letterform proportions; no third-party glyph list, encoding table, or metric file was transcribed |
| SRC-011 | The .NET runtime's compression, cryptography, globalization, XML, and URI APIs | Implementation use | Approved | Platform APIs consumed as APIs; no algorithm, table, or test vector is copied into this repository. `System.Xml.XmlReader` is used for XMP under the hardening in `XmpReader`, and `System.Uri` is the codec's only URI parser (IP-014) |
| SRC-021 | The Unicode Standard 17.0, UAX #9, UAX #15, and the Unicode Character Database, under the Unicode License v3 | Character identities and property definitions: **approved**. Shipping data generated from UCD files is permitted by the licence and carries its **notice requirement**, which must be satisfied in the release artifact before any such table is distributed | Approved, with a notice obligation | IP-013 approved 2026-09-01. No UCD-derived table ships today; the encoding tables are authored under SRC-010 and IP-021 |
| SRC-020 | RFC 1950 (zlib), RFC 1951 (DEFLATE), and the PNG predictor definitions | Implementation of the predictors and confirmation of the compression format the runtime provides; cite the RFCs | Approved | IP-011 approved 2026-09-01. Both RFCs are freely published and state their own patent position; no table or test vector was taken from either |
| SRC-019 | ITU-T T.88 / ISO/IEC 14492, lawfully obtained edition | Segment header and region syntax for the segment reader: **approved**. The MQ coder's probability table, transcribed into `Jbig2ArithmeticStates`, and the generic-region context templates: **pending**, on SRC-017's open question | **Approved for the segment subset; the probability table is pending and was taken ahead of the decision rather than under it.** On 2026-09-03 the arithmetic decoder and arithmetic generic-region decoding were implemented on an explicit instruction to proceed with the row open, on the SRC-016 precedent. **That precedent is weaker here and the difference is recorded rather than glossed:** SRC-016 has a licence to read, because Adobe grants CC BY-ND over its specification, whereas SRC-017's gathered evidence found ITU grants no licence, states no implementation exception, and publishes an address for permission requests. This table therefore sits on the least favourable evidence of the three | IP-008 approved 2026-09-01. Structure, the MMR region path, and — since 2026-09-03 — arithmetic generic regions are implemented. No JBIG2-derived capability may be claimed as supported while this row is pending |
| SRC-018 | ISO/IEC 15444-1 / ITU-T T.800, lawfully obtained edition | Marker and box syntax, the lifting formulations of the 5/3 and 9/7 wavelets, the component transforms, and the tag-tree and packet-header procedures: **approved as structure and procedure**. The EBCOT context assignment tables (Annex D) and their initial states: **pending**, on SRC-017's question. The MQ probability table is shared with SRC-019 and pending there | **Approved for structure; the EBCOT context tables are pending and were taken ahead of the decision.** An entropy decoder was written on 2026-09-03 on an explicit instruction, with this row open. It reuses the MQ decoder written for JBIG2 rather than transcribing a second copy of the probability table — the coder and the table are the same in T.88 and T.800 | IP-007 approved 2026-09-01. Marker syntax, tag trees, packet headers, EBCOT tier-1, the wavelets and the component transforms are implemented. No JPEG 2000-derived capability may be claimed as supported while this row is pending |
| SRC-017 | ITU-T T.4 and T.6, and specifically their white, black, makeup, and two-dimensional mode code tables | Structure and clause citation: **approved**. Reproducing the code tables as constants: **pending**. The tables are transcribed into `CcittFaxTables`, which is the only place in this repository where a normative table could not be authored from an underlying fact instead | **Pending.** Brief at [pdf-src-017-review-brief.md](pdf-src-017-review-brief.md), with the applicable terms now gathered (§5a): ITU's notice forbids reproducing any part of a Recommendation without prior written permission, states no implementation exception, and names `jur@itu.int` for requests | IP-009 cleared the patent position and is silent on this. A Huffman code assignment is arbitrary, so an implementation either reproduces it or does not decode; the question is whether that reproduction is within the specification's terms, and it needs a decision of its own before publication. The brief states exactly what is reproduced — 205 code assignments in one 180-line file — why it could not be authored from an underlying fact as every other table here was, that SRC-018 and SRC-019 defer to the answer, and the dispositions available including removal |
| SRC-016 | Adobe CFF / Type 2 specifications and ISO/IEC 14496-22 Open Font Format, lawfully obtained editions | Structures, clause citation, and the INDEX, DICT, and charset layouts: **approved**. Prose, official test material, and the specification's own example font: **excluded**. The 391 standard strings, reproduced as an ordered table in `CffStandardStrings`: **pending**, on the same *shape* of question as SRC-017 but not on the same terms — a different publisher, a different notice, and a different route to permission ([brief §4a](pdf-src-017-review-brief.md)) | **Approved for structure; the standard-strings table is pending.** The charset reader is written from the structures this row already covered. The standard strings are the second transcribed normative table in this repository, and like the first they could not be authored from an underlying fact: a string identifier is a position in an arbitrary list, so an implementation either reproduces the list or resolves no standard name at all | IP-012 approved 2026-09-01 for inspection. This row was widened on 2026-09-03 to record what the CFF reader implements. A reviewer must decide the standard-strings table before publication. It is adjacent to SRC-017 rather than governed by it: the ITU evidence gathered for that row is about ITU's terms and has not been checked against Adobe's or ISO's, so a decision on SRC-017 that is silent about this row should not be read as covering it. The applicable terms are now gathered in [brief §5b](pdf-src-017-review-brief.md) (2026-09-03): the specification's own notice forbids reproduction without written consent, Adobe separately redistributes that same document under CC BY-ND 4.0, ISO's edition carries ISO's standard notice, and SRC-006 does not carry the table. Until a reviewer decides, no CFF-derived capability may be claimed as supported |
| SRC-014 | Adobe Technical Note #5116, *Supporting the DCT Filters in PostScript Level 2* | Confirm the `APP14` marker layout, its identifier, and the meaning of `ColorTransform` 0, 1, and 2; cite the note. Do not reproduce its prose or tables | Approved | IP-006 approved 2026-09-01 |
| SRC-015 | Third-party JPEG implementations: libjpeg / libjpeg-turbo, Apache Commons Imaging, Java ImageIO | **None.** Registered because the IP-006 evidence record names them as evidence that implementing `APP14` is ordinary open practice. That is issue spotting about the licensing landscape, not an implementation source | Rejected for implementation use | Existence of an open-source implementation is not permission to copy it. No code, table, constant, fixture, or test vector from any of them may enter this repository without its own decision |
| SRC-013 | ISO/IEC 10918-1:1994 / ITU-T T.81, lawfully obtained edition | Confirm the frame processes, entropy modes, precisions, and marker layout the cleared tuple names; cite clauses. Do not reproduce prose, tables, diagrams, or official test material | Approved | IP-005 approved 2026-09-01 for baseline sequential DCT, widened 2026-09-02 to progressive DCT and again to extended sequential. No new source was consulted for either widening: the SOF1 and SOF2 marker layouts were already inside this row's permitted use |
| SRC-012 | ISO 16684-1:2019, lawfully obtained edition | Implementation of the pinned XMP read subset: cite clauses, use the structure; do not reproduce prose, tables, diagrams, or official test material | Approved | IP-004 approved 2026-09-01. The serialization forms and property names implemented are facts about the format; the code expressing them is this project's |

## Contributor provenance declaration

Each PDF-related change records:

- source IDs consulted and the permitted use for each;
- files, tables, fixtures, or generated data added;
- whether any code, prose, constants, or test vectors were adapted;
- the license and notice treatment for adapted material;
- a similarity review against unapproved old Broiler PDF and third-party code;
- reviewer name, decision, and date.

Suggested pull-request statement:

> I used only the approved sources listed as: [IDs]. I did not copy or adapt
> unapproved legacy or third-party PDF code, tables, tests, fixtures, or output
> files. Added generated material is reproducible from the sources recorded in
> this change.

## Similarity review log

| Change | Source IDs | Reviewer | Date | Result |
|---|---|---|---|---|
| Phase 0 governance documents and legacy CLI removal | SRC-002–SRC-005, SRC-007 | Maik Ratzmer (project reviewer, engineering seat) | 2026-08-22 | No PDF implementation code or fixtures introduced; legacy process/test surface removed |
| Base PDF codec implementation (`Broiler.Documents.Pdf`) | SRC-007, SRC-010, SRC-011 | Maik Ratzmer (project reviewer, engineering seat) | 2026-08-25 | No code, table, fixture, or test vector adapted from the retired PdfSharp/PdfPig lineages or from any third-party PDF implementation. Syntax and structure written from the clause structure of ISO 32000-1 without reproducing its text. Encoding and metric data authored under SRC-010. All test fixtures generated in code; no `.pdf` committed. |
| URI handling provenance (`Broiler.Documents.Pdf/PdfUriPolicy.cs`) | SRC-011 | Maik Ratzmer (project reviewer, engineering seat) | 2026-09-01 | A search of the whole codec for URI construction and parsing returns exactly one call site, `System.Uri.TryCreate`. Nothing was adapted from RFC 3986, from any URL library, or from another PDF implementation: there is no grammar, no percent-decoding table, no normalization step, and no test vector. The scheme allow-list, the length ceiling, the control-character rejection, and the user-information rule are this project's decisions, written here and tested here. |
| ISO 32000 source-use and character-data review (SRC-001, IP-021) | SRC-001, SRC-010 | Maik Ratzmer (project reviewer, engineering seat) | 2026-09-01 | A whole-tree inspection: the PDF source directories contain nothing but `.cs` and `.csproj` files, so no standard excerpt, glyph list, encoding table, or metric file has been committed. Behaviour was written from clause structure; the character data is built from literals and loops in code. No PDF 2.0-only construct is implemented, so no ISO 32000-2 text was required for anything that ships. A guard test now enforces the no-data-file property rather than leaving it to be re-established by hand. |
| Symbol built-in encoding (`Broiler.Documents.Pdf/Text/PdfEncodings.cs`) | SRC-001, SRC-010, SRC-021 | Maik Ratzmer (project reviewer, engineering seat) | 2026-09-01 | Authored slot by slot from the character each one denotes, exactly as the existing encoding tables were: no glyph-list file, no UCD file, and no table from any other implementation was read or transcribed. The tests assert each mapping individually so the authored data is reviewed rather than asserted in bulk. Nothing generated from a UCD file is added, so the Unicode License notice requirement does not yet attach. |
| Predictor completion (`Broiler.Documents.Pdf/Filters/PdfPredictor.cs`) | SRC-001, SRC-020 | Maik Ratzmer (project reviewer, engineering seat) | 2026-09-01 | TIFF predictor 2 extended to 1-, 2-, and 4-bit components, written from the horizontal-differencing rule in ISO 32000-1 §7.4.4.4. No code, table, or constant was taken from libtiff, libpng, zlib, or any other implementation; the PNG Paeth predictor is the arithmetic the specification states, not a transcription of anyone's source. Every fixture is generated in the test and round-tripped against a forward predictor written there. |
| Composed `JBIG2Decode` filter (`Broiler.Documents.Pdf.Images`) | SRC-001, SRC-007, SRC-017, SRC-019 | Maik Ratzmer (project reviewer, engineering seat) | 2026-09-01 | Segment header parsing, region placement, and page compositing written from the clause structure of ITU-T T.88 and ISO 32000-1 §7.4.7. No code was taken from jbig2dec, the Xpdf lineage, or any other JBIG2 implementation. The one region type that decodes reuses this repository's own T.6 decoder rather than a second copy of it, so it inherits SRC-017's open question about those tables and adds no new transcription of its own. No `.jb2` file is committed; every stream in the tests is assembled segment by segment. |
| Composed `JPXDecode` reader (`Broiler.Documents.Pdf.Images`) | SRC-001, SRC-018 | Maik Ratzmer (project reviewer, engineering seat) | 2026-09-01 | Marker and box parsing written from the clause structure of ISO/IEC 15444-1 and ISO 32000-1 §7.4.9. No code was taken from OpenJPEG, Kakadu, Jasper, or any other JPEG 2000 implementation, and no normative table was reproduced: the reader needs marker codes and field layouts, not the MQ-coder state table. No `.jp2` or `.j2k` file is committed; every codestream in the tests is assembled marker by marker in the test. |
| Composed `CCITTFaxDecode` filter (`Broiler.Documents.Pdf.Images`) | SRC-001, SRC-007, SRC-017 | Maik Ratzmer (project reviewer, engineering seat) | 2026-09-01 | The decoder, the reference-line handling, the changing-element representation, and the PDF parameter semantics are this repository's, written from the clause structure of ITU-T T.4/T.6 and ISO 32000-1 §7.4.6. No code was taken from libtiff, ghostscript, leptonica, or any other fax implementation. **The run-length and mode code tables are transcribed** and are recorded as such in `CcittFaxTables` and in SRC-017; they are the sole exception to this repository's authored-not-copied position on data. No fax image or test vector comes from the standard or from anywhere else: every bitmap in the tests is generated and encoded by an encoder written in the test suite. |
| Built-in `LZWDecode` filter (`Broiler.Documents.Pdf/Filters`) | SRC-001, SRC-007 | Maik Ratzmer (project reviewer, engineering seat) | 2026-09-01 | Written from the clause structure of ISO 32000-1 §7.4.4. No code, table, or constant was taken from libtiff, ghostscript, zlib's LZW-era relatives, or any other implementation, and the specification's worked example was deliberately **not** transcribed — a normative table is exactly what the source rule keeps out of this repository. The test vectors are produced by an encoder written in the test suite, so the round trip is proved over arbitrary data rather than over one blessed sample. |
| Composed font-program reader (`Broiler.Documents.Pdf.Fonts`) | SRC-006, SRC-007 | Maik Ratzmer (project reviewer, engineering seat) | 2026-09-01 | No font parser was written for this change. The sfnt parser is the project's own in `Broiler.Graphics` (SRC-007), reached through its public API; this change adds only the PDF descriptor handling, the budget, and the inversion of the parser's character map. No code, table, or test vector from FreeType, HarfBuzz, stb_truetype, or any other font implementation was consulted or copied. No font file is committed: the test font is assembled byte by byte from the table structures in SRC-006, and carries no outlines, no name table, and no third-party design. |
| JPEG colour-transform resolution (`Broiler.Documents.Pdf.Images`) | SRC-013, SRC-014 | Maik Ratzmer (project reviewer, engineering seat) | 2026-09-01 | The marker layout and the three `ColorTransform` values were taken from Adobe Technical Note #5116 and the PDF clause for the `/ColorTransform` entry. The named third-party implementations (SRC-015) were not read: the resolution rule, the precedence decision, and the contradiction handling are this project's, and the last of them deliberately differs from what those implementations do. No fixture or test vector was taken from anywhere; the APP14 markers in the tests are assembled byte by byte in the test. |
| Progressive DCT widening (`Broiler.Documents.Pdf.Images`) | SRC-013 | Maik Ratzmer (project reviewer, engineering seat) | 2026-09-02 | No decoder was written and no source was newly consulted. The change removes one frame-marker refusal from `JpegStreamFilter`, so the progressive images `Broiler.Media`'s decoder already handled now reach it. The test file added for it is a progressive JPEG assembled marker by marker in the test — quantization table, Huffman table, SOF2, and a DC-only scan whose six entropy bits are written out in the fixture's own comment — so no `.jpg` is committed and no image, table, or test vector comes from ISO/IEC 10918-1 or from any implementation of it. |
| Composed baseline JPEG filter (`Broiler.Documents.Pdf.Images`) | SRC-007, SRC-013 | Maik Ratzmer (project reviewer, engineering seat) | 2026-09-01 | No decoder was written for this change. The JPEG implementation is the project's own managed codec in `Broiler.Media.Image.Managed` (SRC-007), reached through its public API; this change adds only the PDF-side marker scan, tuple gate, and budget check, written from the marker layout in ISO/IEC 10918-1. No code, table, quantization or Huffman table, fixture, or test vector was taken from libjpeg or any other third-party JPEG implementation. Every test image is encoded in the test by the project's own encoder; no `.jpg` is committed. |
| Composed bare-CFF glyph-name reader (`Broiler.Documents.Pdf.Fonts`) | SRC-007, SRC-016 | **Pending review** | 2026-09-03 | The header, INDEX, DICT, and charset readers are written from the structures SRC-016 covers; no code was taken from FreeType, fontTools, the Xpdf lineage, or any other CFF implementation, and the specification's example font was not read or reproduced. **The 391 standard strings are transcribed** and are recorded as such in `CffStandardStrings` and in SRC-016; they are the second exception to this repository's authored-not-copied position on data, and the first since SRC-017. Nothing decides what a name *means* here: the codec resolves names through its own authored glyph data under SRC-010, so no third-party glyph list entered the repository. No font file is committed; the CFF in the tests is assembled byte by byte from the format's structures and carries one `endchar` per glyph and no outlines. |
| JBIG2 arithmetic decoder and generic regions (`Broiler.Documents.Pdf.Images`) | SRC-001, SRC-007, SRC-019 | **Pending review** | 2026-09-03 | The MQ decoder's procedures, the generic-region decoding loop, the adaptive-pixel handling and the typical-prediction path are written from the clause structure of ITU-T T.88. No code was taken from jbig2dec, the Xpdf lineage, pdf.js, or any other implementation. **The MQ probability table is transcribed** — 47 states in `Jbig2ArithmeticStates` — and is recorded as such there and in SRC-019; it is the third such table here, after `CcittFaxTables` and `CffStandardStrings`. The generic-region context templates are written from the standard's figures and are a second thing a reviewer must check by eye. **No fixture comes from the standard or from anywhere else:** every bitstream in the tests is produced by an MQ encoder and a generic-region encoder written in the test suite, which the tests state proves self-consistency and not conformance, because the standard's own test sequence is official test material and no third-party `.jb2` may be committed (IP-020). |
| JPEG 2000 entropy decoder and wavelets (`Broiler.Documents.Pdf.Images`) | SRC-001, SRC-007, SRC-018 | **Pending review** | 2026-09-03 | Marker parsing, tag trees, packet headers, EBCOT tier-1, the inverse 5/3 and 9/7 wavelets and the component transforms are written from the clause structure of ISO/IEC 15444-1. No code was taken from OpenJPEG, Kakadu, Jasper, pdf.js, or any other JPEG 2000 implementation. **The EBCOT context assignment tables are transcribed** and recorded as such in `JpxBlockDecoder` and in SRC-018; the MQ probability table is not transcribed again, because the decoder reuses the one written for JBIG2 under SRC-019. **The evidence divides and the division is recorded in the tests:** the wavelets and component transforms are tested by inversion against independently written forward transforms, and the tag tree by round trip, which is real evidence; the EBCOT context tables and packet-header field order are tested by nothing, because an encoder able to exercise them would be as large as the decoder and would share its reading. No codestream comes from the standard or from anywhere else: every fixture is assembled marker by marker in the test. |
| XMP read subset (`Broiler.Documents/XmpMetadata.cs`) and its PDF reconciliation | SRC-011, SRC-012 | Maik Ratzmer (project reviewer, engineering seat) | 2026-09-01 | Written from the serialization structure of ISO 16684-1:2019. The Adobe XMP Toolkit (SRC-005) was not consulted for content: no code, schema table, namespace list, fixture, or test vector from it or from any other XMP implementation was read for or copied into this change. The namespace URIs and property names are format identifiers, not copied tables. Every packet in the tests is written in the test. |
